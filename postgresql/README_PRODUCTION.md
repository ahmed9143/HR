# PostgreSQL Production Target

The V10 app stays SQLite-compatible by default. This folder provides the practical PostgreSQL target for a multi-user server deployment.

Use one HR server + PostgreSQL + clients. No microservices are required.
